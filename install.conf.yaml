- defaults:
    link:
      create: true
      relink: true
      force: true

#- clean: ['~', '~/.config']

- link:
    ~/.i3:
    ~/.config/alacritty/alacritty.yaml: alacritty.yaml
    ~/.bashrc:
    ~/.config/nvim: nvim
    ~/.ssh/config: ssh/config
    ~/.gitconfig:
    ~/.gdbinit:
    ~/.i3status.conf:
    ~/.profile:
    ~/.Xmodmap:
    ~/.Xresources:

- shell:
    - [sh ./nvim/update.sh ,Running neovim plugin setup]
